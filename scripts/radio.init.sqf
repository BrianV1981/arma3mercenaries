/*

// init.sqf or a script executed on mission start
if (isNil "currentRadioSound") then {
    currentRadioSound = ""; // Initialize the variable
};

*/
isMusicPlaying = false;
currentRadioSound = "";
currentTrackDuration = 0;